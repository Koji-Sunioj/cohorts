1. run create.sql scripte to create the suitable tables and schema
2. run load_first.sql to load csv file of first purchases in directory to the database
3. run purchases.sql to load csv file of subsequent purchases in directory to the database
4. run jupyter notebook file to view results, which extract the data
   * note that the actual SQL query is inside the notebook
